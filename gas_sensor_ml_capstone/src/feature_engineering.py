"""Feature engineering utilities."""


def add_engineered_features(df):
    """Add justified engineered features.

    Keep project-specific feature engineering here and document the reasoning
    in the notebook before using the resulting features.
    """
    result = df.copy()
    return result
